import React, { useState } from "react";
import AuthContext from "./AuthContext";//provider가 감싸는 곳에 전달할 내용

/*
AuthContext에 저장된 값을 Provider가 감싸고 있는 모든 js에
저장된 값이 적용될 수 있도록 감싸는 js

*/

const AuthProvider = () => {

    return(
        <>
        </>
    )
}
export default AuthProvider;